# Cloudflare Pages 配置
# 用于 Cloudflare Pages 部署，不影响 Vercel
name = "llm-navigator"
compatibility_date = "2025-05-31"

# 静态资源目录（根目录）
[assets]
directory = "./"

# 排除不需要上传的文件
[assets.exclude]
patterns = ["**/api/", "**/backend/", "**/requirements.txt", "**/vercel.json", "**/.git/", "**/.gitignore"]
